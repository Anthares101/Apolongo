<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.apolongo.apolongo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".PurchaseListActivity"
            android:label="@string/title_activity_purchase_list"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".PurchaseActivity" android:label="@string/tittle_activity_Purchase"/>
        <activity
            android:name=".CycleListActivity"
            android:label="@string/title_activity_cycle_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".NewCardActivity" android:label="@string/tittle_activity_NewCard"/>
        <activity android:name=".NewPurchaseActivity" android:label="@string/tittle_activity_NewPurchase"/>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>